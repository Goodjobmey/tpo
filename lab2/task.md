# Лабораторная работа #2

Провести интеграционное тестирование программы, осуществляющей вычисление системы функций (в соответствии с вариантом).

x <= 0 : (cos(x) - sin(x))
x > 0 : (((((log_5(x) + log_10(x)) / log_2(x)) * log_3(x)) / (ln(x) / log_3(x))) - ((log_2(x) ^ 2) + (log_2(x) ^ 2)))

Правила выполнения работы:

- Все составляющие систему функции (как тригонометрические, так и логарифмические) должны быть выражены через базовые (тригонометрическая зависит от варианта; логарифмическая - натуральный логарифм).
- Структура приложения, тестируемого в рамках лабораторной работы, должна выглядеть следующим образом
- Обе "базовые" функции (в примере выше - sin(x) и ln(x)) должны быть реализованы при помощи разложения в ряд с задаваемой погрешностью. Использовать тригонометрические / логарифмические преобразования для упрощения функций ЗАПРЕЩЕНО.
- Для КАЖДОГО модуля должны быть реализованы табличные заглушки. При этом, необходимо найти область допустимых значений функций, и, при необходимости, определить взаимозависимые точки в модулях.
- Разработанное приложение должно позволять выводить значения, выдаваемое любым модулем системы, в сsv файл вида «X, Результаты модуля (X)», позволяющее произвольно менять шаг наращивания Х. Разделитель в файле csv можно использовать произвольный.

---

### **Интеграционное тестирование**  

### 1. 集成测试的目的与任务  
**Цели и задачи интеграционного тестирования**

| 中文 | русский |
|------|----------------------|
| 检查模块（组件）或系统之间的接口及其交互 | Проверяет интерфейсы и взаимодействие модулей (компонент) или систем |
| 验证 API 调用、面向对象组件之间的消息 | Вызовы API, сообщения между ОО компонентами |
| 验证数据库、用户图形界面 | Баз Данных, пользовательский графический интерфейс |
| 验证网络、硬件、本地接口等 | Интерфейсы взаимодействия (сетевые, аппаратные, локальные, …) |
| 验证基础设施接口 | Инфраструктурные |

---

### 2. 集成测试在测试序列中的位置  
**Расположение фазы интеграционного тестирования в последовательности тестов**

| 中文 | русский |
|------|----------|
| 集成测试在单元测试之后，系统测试之前 | Начинается после окончания интеграции (а интеграция — после модульного тестирования) |
| 后续阶段：系统测试、α/β测试、验收测试 | Системное тестирование → Альфа- и Бета-тестирование → Приемочное тестирование |


---

### 3. 集成测试的算法（过程）  
**Алгоритм интеграционного тестирования**

| 中文 | русский |
|------|----------|
| 当两个组件开发或设计完成后即可开始集成 | Может проводиться когда два компонента разработаны (спроектированы) |
| 其余组件按准备情况逐步添加 | Остальные добавляются по готовности |
| 每个集成阶段后执行回归测试 | Использовать регрессионное тестирование после каждого этапа интеграции |
| 自动化测试 | Автоматизировать тесты |

> 📍 стр. 63, 72

---

### 4. 集成测试的策略与概念  
**Концепции и подходы, используемые при реализации интеграционного тестирования**

| 中文 | русский |
|------|----------|
| 自顶向下 | Сверху вниз |
| 自底向上 | Снизу вверх |
| 功能集成（端到端） | Функциональная (end to end) — по одной функции |
| 核心集成 | Ядро (backbone) |
| 大爆炸集成 | Большой взрыв (big bang) |

**基本原则**：

| 中文 | русский |
|------|----------|
| 优先集成最复杂组件，降低风险 | Интегрировать в первую очередь наиболее сложные компоненты – Снизит риски |
| 根据开发顺序选择集成顺序 | Выбирать порядок интеграции с учетом порядка разработки |
| 每个集成阶段后执行回归测试 | Использовать регрессионное тестирование после каждого этапа интеграции |
| 自动化测试 | Автоматизировать тесты |

---

### 5. 用于集成测试的软件产品  
**Программные продукты, используемые для реализации интеграционного тестирования**

#### 5.1 JUnit 用于集成测试  
**Использование JUnit для интеграционных тестов**

| 中文 | русский |
|------|----------|
| JUnit 提供注解标记测试方法 | Аннотации для маркировки метода как теста @Test |
| 提供 @Before / @After | Аннотации для маркировки действий до и после теста |
| 提供断言方法 | Методы для проверки (assertion) |


#### 5.2 Mockito 用于模拟对象（桩和模拟）

| 中文 | русский |
|------|----------|
| 虚拟对象（传递但不使用） | dummy object |
| 简化实现的工作对象 | fake objects |
| 部分实现，用于调用其方法 | stub |
| 预定义值的简单实现 | mock object |

**示例**：

- 创建模拟对象：`mock(MyClass.class)`
- 定义返回值：`when(...).thenReturn(...)`
- 根据参数返回不同值：`when(c.compareTo("Mockito")).thenReturn(1)`
- 根据参数类型返回：`when(c.compareTo(isA(Todo.class))).thenReturn(0)`

---

### 6. 集成测试的自动化  
**Автоматизация интеграционных тестов**

| 中文 | русский |
|------|----------|
| 自动化是集成测试的关键原则 | Автоматизировать тесты |
| 可使用 JUnit + Mockito 编写自动化集成测试 | JUnit + Mockito |
| Selenium 可用于 Web 集成测试 | Selenium — тестирование web-приложений |
| JMeter 可用于接口集成测试 | Jmeter → Функциональное тестирование|